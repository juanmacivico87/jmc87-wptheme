{
    "name": "wptheme/sample",
    "description": "{{ theme_description }}",
    "require": {
        "php": ">=7.2"
    },
    "autoload": {
        "psr-4": {
            "PrefixConfig\\": "config/",
            "PrefixSource\\": "src/"
        }
    }
}